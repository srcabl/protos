syntax = "proto3";

package srcabl.shared;

option go_package ="github.com/srcabl/protos/shared";

import "shared/source.proto";

message SourceNode {

    // Source is the source of this tree node
    Source source = 1;

    // Children are the 
    repeated Source children = 2;
}